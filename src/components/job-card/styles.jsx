import { makeStyles } from "@material-ui/styles";

export default makeStyles((theme) => ({
  root: {
    padding: "15px",
    marginTop: "20px",
  },
  bullet: {
    listStyleType: "disc"
  },
  date: {
    fontStyle: "italic"
  }
}));
